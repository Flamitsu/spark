use crate::IgnixError;
/// This function should remove the spark installation in the current ESP partition.
pub fn remove_spark_installation(_confirmation: bool) -> Result<(), IgnixError>{
    todo!("Remove the Ignix installation in the ESP and the NVRAM");
}
