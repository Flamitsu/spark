[package]
name = "ignix-cli"
version.workspace = true
edition.workspace = true
description = "Managment tool for configure and install the Ignix bootloader."
license.workspace = true

[[bin]]
name = "ignix"
path = "src/main.rs"

[dependencies]
