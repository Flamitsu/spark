use crate::SparkError;
/// Extracts the EFI binary path in the argument that have been provided.
pub fn get_efi_bin_path() -> Result<(), SparkError>{
    todo!("Extracts the EFI binary path in the argument.");
}
